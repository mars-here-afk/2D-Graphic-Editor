# 2D-Graphic-Editor
Can draw a line, rectangle, triangle, and a circle. It can also delete or modify the objects present in the canvas. It also has a feature to display these objects.
The object's outline is marked by an asterisk symbol (*) and the background as an underscore (_).
